# Optional: enables AI-powered photo solving (reads handwriting/diagrams/word
# problems via a Cloudflare Workers AI-backed proxy — see worker/README.md
# for the free one-time setup). If left unset, the app works exactly as
# before: on-device OCR + rule-based solver only, no network dependency.
VITE_AI_SOLVE_ENDPOINT=
VITE_AI_SHARED_SECRET=
